// Demo state: intentionally empty.
//
// Used in Module 3.2 (Comment-Driven Development demo). The trainer
// progressively adds:
//   Style A — a one-line comment ("// Calculate VAT")
//   Style B — a full spec block with @param, @returns, edge cases
//   Style C — spec + scaffold (function signature with // comments inside)
//
// Delete this entire file's content live during the demo, then build it
// up in three styles.
