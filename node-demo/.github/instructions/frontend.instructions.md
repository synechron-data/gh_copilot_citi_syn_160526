---
applyTo: "apps/web/**/*.{ts,tsx}"
---

For all frontend code:

- Use React functional components with hooks.
- Prefer `useQuery` / `useMutation` from React Query.
- Use Tailwind utility classes, no inline styles.
- Always add basic accessibility attributes (aria-*, roles, etc.).
